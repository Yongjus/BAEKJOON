N = int(input())

cards = [i for i in range(1, N+1)]

put_under = False
while len(cards) > 1:
    if put_under:
        cards.append(cards.pop(0))
    else:
        cards.pop(0)
    put_under = not put_under
print(cards[0])
